import React from 'react';
import ReactDOM  from 'react-dom';
import App from './App';

ReactDOM.render( // Render the App component into the root element.
    <App />,
    document.getElementById('root') 
);
